{
  "name": "ova",
  "version": "1.0.0",
  "description": "",
  "main": "src/main.jsx",
  "scripts": {
    "build": "esbuild src/main.jsx --bundle --outfile=dist/app.js --loader:.jsx=jsx --format=iife --global-name=OVAApp --minify",
    "build:watch": "esbuild src/main.jsx --bundle --outfile=dist/app.js --loader:.jsx=jsx --format=iife --global-name=OVAApp --watch",
    "start": "node server/index.js",
    "serve": "npm run build && node server/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "express": "^4.21.2",
    "sql.js": "^1.12.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
